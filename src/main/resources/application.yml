spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 500
        min-idle: 0
    lettuce:
      shutdown-timeout: 0
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/procedure
    username: root
    password: rootmysql
    type: com.alibaba.druid.pool.DruidDataSource

mybatis-plus:
  typeAliasesPackage: cn.edu.mju.procedure.pojo
  mapper-locations: classpath*:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true          #true开启驼峰命名

server:
  port: 8081
